import { Tooltip } from "react-bootstrap";

export const tooltipEdit = (
  <Tooltip id="tooltip">
    Editar
  </Tooltip>
);

export const tooltipDelete = (
  <Tooltip id="tooltip">
    Excluir
  </Tooltip>
);