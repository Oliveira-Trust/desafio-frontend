
import Header from '../components/header/header';
import Wallets from '../components/wallets/wallets';

const Home = ( ) => {
  return (
      <main>
        <Header />
        <Wallets />
      </main>
    );
  }

export default Home;